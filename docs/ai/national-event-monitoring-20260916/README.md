# 全国イベント監視：Work再開用ハンドオフ

Status: **同じPR #21で継続 / オフライン実装・検証済み / 全国台帳審査中 / 未移設・未公開**。この文書は再開記録であり、既存specや公開条件を上書きしない。

## 2026-09-22の会場追加・日時訂正（最新）

同じPR #21の `c3d6b60` から継続。mainは `c1236cf` のままで追加merge不要。R1〜R3の修正を維持し、全国台帳を123会場へ補い、日時訂正後に古い公演が残る問題と、後日復活する問題を修正した。**全309 tests / 70 subtests成功**。日時訂正は合成公演、実公演は長崎の1件追加を一時DB→LP→manifestで検証。本番・定期Chat/Work Cloudは未検証。詳細は [VERIFICATION_20260922.md](VERIFICATION_20260922.md) 冒頭。

| 段階 | 今回の追加結果 | 残る条件 |
|---|---|---|
| A | 台帳123 / 登録25県 / 公式有効32 / watch12 / Ticketjam75（68有効）。衝突・孤立なし | 本番適用はHの条件後 |
| B | 運営者で確認した7会場を追加。全47県131観測、元116会場維持。容量不明7会場・工事/休館予定を保持 | 未審査108、追加確認21、住所相違1、全国の追加突合 |
| C | 公式予定表の見えた範囲を記録し8分割一覧更新。既存全国ツアーfixture維持 | 全会場の予定表・全国発表元・SNS、実巡回 |
| D | 同一公演の開催日・終了日・開始時刻だけの訂正を実source/LP照合付きの取込案へ接続。旧行保持、別公演保持、古い日時の非復活 | 実体変更・連続訂正・承認された実取込 |
| E | 日次公平性・再試行・排他を含む既存テストを再実行 | Cloud/Actions/旧端末の単一writer、実使用量・遅延 |
| F | 新規・中止延期・日時訂正を一時DB→LP→manifestで検証。CLI原本非変更も確認 | 定期提出・Work自動起動・承認待ち・再実行の実測 |
| G | 全pytest成功。今回7会場追加による既存LPの追加差分0。再生成1,144→実公演試験1,145、validator成功 | 最新base・本番取込版、Release・実LP |
| H | 公開・権限・既存タスク・旧端末は未変更 | Cloud受入・切替承認・旧writer停止確認・公開hash追跡 |

7会場はニプロハチ公ドーム、ビッグハット、エムウェーブ、アスティとくしま、愛媛県武道館、グランメッセ熊本、滋賀ダイハツアリーナ。取得処理は有効化していない。定期実行の成功と会場調査の成功を分ける。日時訂正の `add_correction` / `ready_for_review` も承認待ちの案であり、本番へ適用していない。

## 2026-09-22の追加続行結果（履歴）

main `c1236cf` を同じPRへ通常mergeし、R1〜R3の修正を維持して続行。**全289 tests / 70 subtests成功**。純粋な中止・延期の無公開取込案を既存のLP抑止へ接続し、別時刻の公演保持と古いsource再入力時の非復活を一時DBで確認した。あいみょん全国36公演の会場保留6件を解消。詳細は [VERIFICATION_20260922.md](VERIFICATION_20260922.md) 冒頭。全国監視・定期移設・本番公開は未完了。

| 段階 | 追加結果 | 未完了条件 |
|---|---|---|
| A | 最新DB更新を保持。台帳116 / 公式有効32 / watch12 / Ticketjam75（68有効）、衝突・孤立なし | 本番適用はHの条件後 |
| B | 全47県の128観測を保持し2観測追加。日本ガイシ・朱鷺メッセの既存IDで名称対応 | 114 pending、14追加確認待ち、1住所相違、全国の追加突合 |
| C | あいみょん全36公演を対応。ホール予定表で4公演を逆照合。8分割snapshot更新 | 全会場の予定表・全国発表元・SNS、実巡回状態 |
| D | 中止・延期だけの状態変更を別IDの抑止案として出力。旧行・conflict履歴を保持 | 日時等の一般訂正、承認された実取込接続 |
| E | 日次公平性・再試行・重複/排他の回帰検証 | Cloud/Actions/旧端末の共有排他、使用量・遅延 |
| F | 4通りの状態変更を一時DB→LP→manifestで検証。実公演1件の追加を最新baseで再検証 | 定期提出・Work自動起動・承認待ち・再実行の実測 |
| G | 全pytest成功。最新LP1,142→再生成1,144→実公演試験1,145。validator成功 | Release・実LP、公開直前の最新base再検証 |
| H | 既存タスク・権限・旧端末は未変更 | Cloud受入・単一writer・切替許可・旧writer停止・公開hash追跡 |

今回の別名修正による追加の既存LP差分は0。前回の台帳追加によるIG +2公演とTOYOTA 2公演の表示名/key差は最新DBにも残る。中止・延期の試験は合成公演であり、実公演の中止を新たに登録していない。新規/抑止案の `ready_for_review` は本番承認ではない。

## 2026-09-22の前回続行結果（履歴）

最新main `70989a8` を同じPRへ通常merge。会場台帳を107→116件へ補い、提案から原本を変えずに取込案を出す機能を追加した。**全276 tests / 70 subtests成功**。公式で確認した長崎の1公演を一時DB→LP→manifestまで試験し、初回1件・再実行0件を確認。定期Chat/Work Cloud・本番公開は未実施。詳細は [VERIFICATION_20260922.md](VERIFICATION_20260922.md)。下の記録は履歴。

| 段階 | 今回の結果 | 未完了条件 |
|---|---|---|
| A | 最新DB・configを保持して再点検。116台帳 / 32公式有効 / 12watch / 75Ticketjam（68有効）、衝突・孤立なし | 本番適用はHの条件後 |
| B | 9会場を追加、19県に登録。47県128観測を維持。ポートメッセ第1展示館は容量不明でも別IDで保持 | 114 pending、12追加確認待ち、1住所相違、全国の追加突合 |
| C | 8分割snapshot更新。SEKAI全23公演を対応。あいみょん全国36公演を全件fixture化、30対応・6保留 | 全予定表・全国発表元・SNS。保留6公演の会場突合 |
| D | config/現行LPの衝突検査を通して新規公演の取込案を出力。再入力はunchanged | 訂正・中止延期の旧source/旧UID移行、承認された実取込接続 |
| E | 日次公平性・再試行・重複/排他を再テスト | Cloud/Actions/旧端末間の共有排他、実使用量・遅延 |
| F | 実公演1件を一時DB→LP→manifestで無公開検証 | 定期提出・Work自動起動・承認待ち・再実行の実測 |
| G | 全pytest成功。台帳追加でLP1,132→1,134、試験公演追加で1,135。validator成功 | IG地域保留解除2件・TOYOTA表示名/key変更2件の本番影響再確認、Release・実LP |
| H | 既存タスク・権限・旧端末を変更せず、承認条件を維持 | Cloud受入・単一writer・切替許可・旧writer停止・公開hash追跡 |

B/Cは全国完了ではなく、D〜Gは後続の準備と無公開試験。`--prepare-import` の `ready_for_review` も本番承認ではない。`blocked` はconfig=nullのまま保持し、訂正を新規行追加へ読み替えない。

## 2026-09-21のレビュー修正・続行結果

[ハンドオフレビューR1〜R3](https://github.com/NemuKei/market-stats-viewer/pull/21#pullrequestreview-5260662904)を実repoの回帰テストで再現して修正した。日次容量不足時の巡回の偏り、Ticketjamの訂正受渡し、再確認日のJST判定を修正。関連181 tests / 28 subtests成功。詳細は [VERIFICATION_20260921.md](VERIFICATION_20260921.md)。下の9月20日・16日の数値は履歴として保持する。

| 段階 | 今回進めたこと | 未完了条件 |
|---|---|---|
| A | 最新mainを通常mergeし再点検。107台帳 / 32公式有効 / 12watch / 75Ticketjam（68有効）、衝突・孤立なし | 本番適用はHの条件後 |
| B | 運営者で確認した広島・福井・エコパスタジアムを追加。元104会場を維持。47県127観測 | 122観測の施設審査、4観測の追加確認、全国の追加突合。127は網羅率の分母ではない |
| C | 全県snapshot更新。実ツアー23公演中21公演を会場対応。LDH全国12公演の逆照合を追加 | 全予定表・全国発表元・SNS投稿取得。ポートメッセ第1展示館2公演の対応 |
| D | 旧候補conflictと訂正下書きを分離。実LPの元key/fingerprintを維持する他経路の訂正受付を追加 | Workの実確認、元source/公式configへの承認された取込接続 |
| E | 全日次履歴の試行順とJST再試行を実装。未試行を優先し残数・待機・遅延を分離 | Cloud/Actions/旧端末の共有排他、使用量・実遅延 |
| F | データ受渡しと合成DB→LP→manifest試験を再実行 | 定期Chat提出・Work自動起動・承認待ち・再実行の実測経路が未接続 |
| G | 最新DBコピーからLP1,149件、現行events配列一致。manifest/validator成功 | 本番取込版の再検証、Releaseと実LP |
| H | 承認境界を維持 | Cloud受入・単一writer・切替許可・旧writer停止確認・公開hash追跡 |

R1〜R3修正とオフライン検証を、A〜H全体の完了とは扱わない。B/Cの審査を続け、Dの取込接続とE/Fの実経路を検証してからHへ進む。現時点のWork Cloudトリガー操作・結果は確認できておらず、定期経由の成功を主張しない。

## 2026-09-20の続行結果

root AGENTSと本ハンドオフを読み、実repoを取得してAから点検した。利用側の目標は全国の3経路から公式確認済みの公演をLPへ届けること。今回の検証可能な成果は、会場同一性の修正、全国の調査待ち一覧、受入・巡回状態のオフライン実装、既存生成経路との接続試験。**本番の縦断受入はまだ完了していない**。

| 段階 | 実施結果 | 残る受入条件 |
|---|---|---|
| A | 原本3設定・保存DB行を点検。国立と味の素のwatch/表示名を訂正。参照サイトだった4公式URLを訂正。既存IDと保存行を維持 | 本番適用はHの条件後 |
| B | 登録済み104会場を維持。競技団体・公式ツアー・運営者から47県124件の県/名称別観測を収録 | 全国施設の実体・別名・住所・規模・稼働状況の審査と自治体資料との追加突合。台帳完成とはしていない |
| C | 8分割の読取snapshot、保存公式URL・前回状態・未処理提案の受渡しを実装。既存104公式URLも再取得 | 全予定表範囲の閲覧、発表元の全国リスト、SNS投稿取得の失敗解消、実状態の継続更新 |
| D | データ専用の提案受付、古い版・未知会場・根拠不一致の停止、既存判断validatorの再利用を実装 | Workの本人性確認、既存configとの衝突審査、承認された取込経路の接続 |
| E | JST日次key、最古未確認優先、残数、重複候補、同一filesystemの排他を実装・試験 | Cloud/Actions/旧端末をまたぐ単一writer、使用量・最大遅延の実測 |
| F | 合成提案から一時DB→LP→manifestまで無公開試験 | 定期Chatの提出・Work自動起動・承認待ち・再実行の実測。現在の対話操作で代用しない |
| G | 既存6focusedを含む144 testsと28 subtests成功。実ツアー23公演をfixture化。最新mainのDBコピーからLP1,183件を再生成し内容一致・manifest/validatorを確認 | 本番取込版を最新baseで再検証、Releaseと利用側の実確認 |
| H | 承認・検証条件を維持。切替していない | Cloud受入、切替許可、旧writer停止確認、公開hashと実LPの追跡 |

Bが未完了のため、C〜Gは後続受入の準備と無公開試験にとどめる。詳細・入力hash・未実施事項は [VERIFICATION_20260920.md](VERIFICATION_20260920.md)。次はBの `missing_fields` とCの取得失敗を解消し、全国台帳と発表元を確定してから定期経路の受入へ進む。台帳・URL修正のPR差分は今回の続行指示の範囲で作成し、mainへの適用・公開・権限・旧端末設定は変更していない。

## 2026-09-16時点の準備記録

以下の「今回」は9月16日時点の記録。未実施の更新状況は上の表と検証記録を参照する。

## 最初に読むもの

1. root `AGENTS.md`
2. このREADME → `COVERAGE.md` → `TASK_PROMPTS.md`
3. `docs/context/PROJECT_CONTEXT.md` のAlways Read Block
4. 必要箇所のみ `docs/spec_data.md`, `docs/spec_update_pipeline.md`, `docs/spec_event_status.md`, `docs/ticketjam_official_review_automation.md`

**目的**：全国のドーム級・アリーナ級・スタジアム級を対象に、会場公式の日程と発表直後の速報を取り込み、レベニュー判断・公開LPにつなげる。Chatを検知・提案、Work Cloudを検証・取り込み・公開担当とする。Ticketjamは第3の補完経路であり、全体の起点ではない。最終対象を大阪・12会場に限定しない。

## 今回の完成物

- 既存104会場の設定棚卸し、13都道府県への偏りと34県の未棚卸しを明示。
- 3経路の分担、共有状態・提案の受け渡し・切替条件を記載。
- `scripts/audit_national_event_coverage.py`：既存3設定を読むだけのオフライン点検。通信、DB更新、Git操作、設定変更を行わずJSONを標準出力へ出す。
- `tests/test_national_event_coverage.py`：合成データによる23テスト。今回の一時環境で成功（Python 3.13.5）。実リポジトリ全体のテスト成功とは別。
- 47都道府県の再調査入口、台帳未登録の公式サイト・予定表候補を記録。
- Chat3系統とWork受入の指示案。**登録・有効化していない**。

## 今回の境界と検証

調査基準commit: `7eadb66a169076504f1b90d6975066c882444d8c`。

GitHub接続で現行ファイルを読み取れたが、一時シェルからの公開GitHub直接取得はDNS失敗。既存repo一式を取得していない。旧試験の曖昧な説明を実測として継承しない。新規点検スクリプトのテストだけを、このPRに収録する同じファイルで実行した。

実施済み: 新規テストの未実装時失敗→実装後23件成功、Python構文検査、UTF-8/BOM/改行/差分点検。
未実施: 既存6focused tests、実3ファイルを入力した点検スクリプト実行、実候補の新規抽出、Chat定期実行からのIssue/PR提出、Work Cloud実行、生成物/Release/実LPの検証、使用量測定。

変更対象は新規の点検コード・テスト・提案文書だけ。`data/**`、依存、DB schema、`.github/workflows/**`、公開field/source policy、認証・権限には変更しない。mainへ取り込まず、workflow dispatchをせず、Release/SideBiz公開をしない。既存端末の設定・未commit作業にも触れない。

## Workの再開手順

このPRを作業の入口に使い、新しい設計会話や子タスクを増やさない。まず実行場所がCloudであることと、必要なrepoにアクセスできることを実測する。現在のChatでできる操作が定期実行でも使えると仮定しない。

```sh
git status --short --branch
git rev-parse HEAD
python --version
git --version
python -m unittest discover -s tests -p 'test_national_event_coverage.py' -v
python -m scripts.audit_national_event_coverage > /tmp/msv-national-event-coverage.json
```

点検の終了コード: 0=設定のレポート作成、1=ID衝突/未登録IDあり、2=入力欠損/不正。**0でも全国監視・取得成功・公開承認を意味しない**。入力hashと基準commitを併記する。

### 仕上げる順番と受入条件

- [ ] **A. 実データ点検を再実行**：104/32/12/75/68は調査時点の値。最新差分があれば再計算し古い値へ戻さない。`mufg_stadium`と`national_stadium`の対応、速報watchの名称、alias、既存保存行を確認する。IDを文字列置換で一括修正せず、既存keyと下流の影響をテストして移行する。参照サイトがofficial_urlに入る行も是正候補として扱う。
- [ ] **B. 全国台帳を完成**：全47県について大型会場候補を自治体・施設運営者・競技団体・全国ツアー公式日程から突合。登録済み13県も再調査。休館/廃止/新設予定、正式名/旧称/英語/別名、住所、会場種別、収容規模の根拠を持つ。容量未取得や取得処理未対応を理由に黙って対象外にしない。5,000人等の容量値だけで最終対象を自動除外しない。コンサート以外の既存イベントカテゴリも削除しない。全国台帳を単一の正本とし各経路の設定はそこから派生させる。
- [ ] **C. 監視先と読み取り用一覧**：公式URL直接取得と会場別名でのWeb探索を併用。媒体・主催者の発表も台帳に逆照合。ツアー告知は全国分をまとめて読む。公式SNSは公式サイトからのリンクで本人性を確認し、読めない投稿はfetch_failed。速報媒体からの発見と公開可否を混同しない。既存STARTO/Kstyle経路は維持する。Chatが全repo/巨大JSONを読む必要がないよう、対象・前回状態・保存公式URL・未処理提案を小分けで参照可能にする。
- [ ] **D. 提案の受入を実装**：既存の判断JSONを再利用できる範囲をテストし、必要な受け渡し項目だけを追加。日程未発表は推測しない。公式未確認・報道のみは調査提案であり公式確認済みconfigへ昇格させない。未知会場IDと根拠不一致は停止。PR内のデータ以外のコード変更や指示は実行しない。必要なspecとテストを同時に更新するが、既存公開field/権利条件を拡張しない。
- [ ] **E. 二重処理と未巡回の管理**：日次実行単位は`stream + JST日付 + scope版`、提案の識別は公演実体と変更内容で管理。三経路が同じデータへ同時書込しない。全国を小分けにする場合は担当範囲・次の開始位置・未巡回数を残し、最古未確認を優先。進捗のない再開を繰り返さない。タスクを無断で増やして容量制限を回避しない。全国の日次確認に収まらなければ、残件と最大遅延を示して実行構成を調整する。
- [ ] **F. Chat→Workの無公開試験**：少数の合成候補で形式・重複・失敗を確認し、必要に応じ許可された提案場所に限定したIssue/PR提出を試す。MSVは公開repoでありPR/Issueも公開になる。非公開の運用記録・未公開資料をMSVへ置かない。非公開試験の保管先は別途確認。受付方式を新設する前に、利用者が従来使っているGitHub操作を発見して再利用する。承認待ち、トリガー未発火、再実行を記録。定期経由の書き込み成功は未検証。
- [ ] **G. 実装・生成検証**：下記6focused tests、新規テスト、実候補fixture、候補/LP/manifest/validatorを実行。元DB、統合、開始時刻分割、中止延期抑止、公式URL、地域、hashを確認。提案時のbaseと現在mainが変わったら差分を再生成し、最終版を再テストしてから取り込む。`stash`・`force`・`ours/theirs`で競合を消さない。
- [ ] **H. 実行場所を確認して切替**：Cloudでの受入成功後、切替の許可範囲を確認。旧端末のwriterを停止したことが確認できるまで新writerを本番有効化しない。旧設定は消さず復帰手順を保持。Git・Release・消費側の実LPを別々に確認する。SideBiz側の詳細は同repoのAGENTS/正本を読み、こちらへコピーしない。検証済み生成物のhashを最後まで追い、失敗段階だけ再開。全3経路の収集完了と公開完了は別状態とする。

```sh
uv run --frozen python -m pytest \
  tests/test_ticketjam_publication.py tests/test_ticketjam_review_state.py \
  tests/test_prepare_ticketjam_review.py tests/test_ticketjam_official_checks.py \
  tests/test_validate_external_events.py tests/test_ticketjam_context_conflict.py \
  tests/test_national_event_coverage.py -q
```

依存のインストールは既存lockに従う。独断で依存定義・認証・権限を変えない。公開コマンドはこの文書から機械的に実行せず、現行specと明示された承認範囲を確認する。

## 完了の定義

全国台帳の対象/対象外/未確認理由が明らかであること、各経路で対象数=確認済み+失敗+未巡回として集計できること、速報の発表日時・初回検知・通知・反映日時を別に測れること、取得失敗を「新規なし」にしないこと、Chat提出からWork受入まで無人経路を実測すること、PCを使わずGit/Release/実LPを確認できること。網羅率の分母は審査済みの対象台帳であり、Web上の全イベントを100%捕捉したとは表現しない。

## 最小の再開指示

> このPRの`docs/ai/national-event-monitoring-20260916/README.md`を入口に、全国イベント監視の仕上げを進める。準備済みコード・調査結果を再利用し、Aから順に実測する。全国対象を縮小しない。未実施を成功と扱わず、公開・権限変更・旧端末停止の承認境界を維持する。結果は実装、テスト、Git、Release、実LP、残課題を分けて報告する。
