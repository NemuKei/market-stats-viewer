# 全国イベント監視：本番切替の受入状況（2026-09-23 UTC）

この記録はDraft PR #21の作業状態を示す。PRのコード、台帳、試験用DBから本番の定期監視・公開を推定しない。

| 条件 | 現在確認できたこと | 判定 |
| --- | --- | --- |
| 監視対象と提案形式 | 209会場・47都道府県、133件の施設観測。全予定表の巡回は未完了 | 一部確認 |
| 生成データの安全なpush | 6つの定期更新workflowから強制rebaseを外す変更をPRへ追加。競合した生成物のpush停止を一時Git remoteで試験。全311 tests / 70 subtests成功 | PR内のみ |
| Cloudでの受入 | Cloud環境の本人性・repo読取、定期Chat提出→Work起動→承認待ち→再実行を同じ経路で測った記録がない | 未確認 |
| 書込担当の一元化 | Actionsのローカルconcurrencyだけでは旧端末・Cloudと排他されない。旧端末の実行設定・停止状態は未確認 | 未確認 |
| 公開の連続性 | Release `external-events-latest` は2026-09-22 16:03 UTC更新。PR内の新機能による更新ではない。利用側の実LP表示も未照合 | 未確認 |

## 切替時の順序

1. PR #21のコードレビューとCloud環境のrepoアクセスを確認する。機密を含まない少数の合成候補で、**実際の定期Chat提出から**Work受入、承認待ち、失敗と再実行を公開なしで記録する。手動の対話や`@codex`コメントだけで定期経路の成功としない。
2. 旧端末の書込処理とmain上で進行中の旧版Actionsを特定する。既存の処理を終えてから旧writerの停止を確認し、対象のDBとLPを更新できる担当を一つに決める。PR内のpush検査は書込権の排他ではなく、競合時は生成runが失敗する。
3. 切替承認の範囲（新writerの有効化、`data/**`生成、workflow dispatch、Release、SideBiz反映）を確認する。承認前には新writerの本番書込を有効化しない。旧設定と復帰手順を保持する。
4. 承認された取込だけを最新mainから再生成し、DB→LP→manifest→validatorを同一baseで通す。競合時は古い生成物をrebaseせず、最新baseからもう一度生成・検証する。
5. Gitのcommit、Releaseの4 assetとmanifest hash、SideBiz取込結果、実LPの表示件数・内容を個別に確認する。Release assetの置換中に消費側が検証エラーになった場合は、混合版を使わず最新の整合した版から再取得する。

PRのマージだけでは`data/events.sqlite`などの公開対象資産は更新されない。上のCloud実測・単一書込者・承認・公開照合が揃うまで「本番切替済み」にしない。
